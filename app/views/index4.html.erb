<p id="notice"><%= notice %></p>
<h1>製品コメント</h1>

  <div class="col-md-8">
      <h3> 件数 (<%= @books.count %>)</h3>
      <ol class="microposts">
        <table>
          <thead>
            <tr>
              <th>名前</th>
              <th>年代</th>
              <th>性別</th>
              <th>意見・感想</th>
              <th colspan="3"></th>
            </tr>
          </thead>

          <tbody>
           <% @books.each do |book| %>
            <tr>
               <td><%= book.name %></td>
               <td><%= book.age %></td>
               <td><%= book.gender %></td>
               <td><%= book.message %></td>
               <td><%= link_to 'Show', book %></td>
               <td><%= link_to 'Edit', edit_book_path(book) %></td>
            </tr>
           </tbody>
        </table>
      </ol>
    <% end %>
  </div>

<br>

<%= link_to 'New Book', new_book_path %>

